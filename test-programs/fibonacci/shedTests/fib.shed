package shedTests;

fun fib(n: Double) : Double {
    if n.equals(0) {
        return 0;
    } else if n.equals(1) {
        return 1;
    } else {
        return fib(n.subtract(1), n.subtract(2));
    }
}

print(fib(8).toString());
