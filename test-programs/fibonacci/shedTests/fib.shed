package shedTests;

def fib(n: Double) : Double => 
    if n.equals(0)
        0
    else if n.equals(1)
        1
    else
        fib(n.subtract(1)).add(fib(n.subtract(2)))

print(fib(8).toString());
