package shedTests;

def fib(n: Double) : Double => 
    if n.equals(0)
        0
    else if n.equals(1)
        1
    else {
        val a = fib(n.subtract(1));
        val b = fib(n.subtract(2));
        return a.add(b);
    }

print(fib(8).toString());
