def fib fun(n: Double) : Double => 
    if n.equals(0) then
        0
    else if n.equals(1) then
        1
    else do {
        val a = fib(n.subtract(1));
        val b = fib(n.subtract(2));
        return a.add(b);
    }

print(fib(8).toString());
